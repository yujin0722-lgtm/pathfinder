# CLAUDE.md — パスファインダー（Pathfinder）開発引継書

このファイルは、claude.ai のチャットで行われた構想段階の議論を Claude Code に引き継ぐためのものです。
新しいセッションでは、まずこのファイル全体を読んでから作業を始めてください。

---

## 1. このプロジェクトは何か

**パスファインダー（Pathfinder）**は、医学論文のエビデンスを「自分の地図」として構造化するためのウェブアプリです。

開発者 Yujin が既に公開している姉妹アプリ **Citrail ―引用を辿る―**（引用文献ネットワーク可視化アプリ、GitHub Pages で公開済み、別リポジトリ）と連携して使うことを想定していますが、独立したアプリとして開発します。

### 背景と目的

- Citrail は 1 本の論文を起点に引用・被引用ネットワークを**網羅的**に表示するが、そこには臨床疑問と無関係な論文も混じる（例：抗菌薬の効果を知りたいのに、統計手法の論文が引用に含まれる）。
- 論文を読む最終目的は、エビデンスを統合して**自分の知見を持つ**こと。
- そこで、Citrail で集めた「足跡」の中から臨床的に意味のある論文を**人間が選び、意味づけしてつなぎ直す**ためのツールがパスファインダー。

### 両アプリの対比（コンセプトの要）

| | Citrail | パスファインダー |
|---|---|---|
| 探索の性格 | 網羅的・受動的 | 選択的・能動的 |
| 図の意味 | 事実としての引用関係 | 自分で作る「エビデンスの地図」 |

### 思想の核・独自性（最重要）

**「事実の関係」と「人間の意味づけ」を、区別しながら同じ地図に重ねられること。**

- 自動リンク：ノードに DOI を入れると論文情報を自動取得し、DOI 入りノード同士は OpenAlex の引用・被引用関係に基づいて自動で線が引かれる（＝事実）。
- 手動リンク：ユーザーが自分の解釈でノード間に線を引ける（＝意味づけ）。
- 両者は見た目で区別できるように表示する（区別の方法は未決定・要検討）。

この二層構造が、単なる引用図エディタとの違いです。設計判断に迷ったら、この思想に立ち返ってください。

---

## 2. 決定済み事項

- 土台のイメージは、Yujin が以前 Claude と作った**ループ図アプリ**（ノード・矢印・メモ・ラベルを手動で自由配置できるエディタ）。これに論文機能を足す。
- ノードに DOI を入力すると論文情報を自動取得。複数の DOI 入りノード間は OpenAlex で引用関係を自動判定して線引き。
- 手動リンク機能あり。自動／手動の線は視覚的に区別する。
- まっさらな状態からも開始できるが、Citrail からのインポート開始も可能にする：Citrail 側に「パスファインダーを開始」ボタン → ノードを複数選択 → 選んだノードが配置済みの状態でパスファインダーが起動、という連携を想定。
- Citrail とは別リポジトリ・別アプリとして開発（大規模開発になるため独立プロジェクト化した）。

## 3. 未検討の論点（ここから議論を再開する）

チャットでは次の 4 論点を挙げ、**論点 1 から議論を始める**ことで合意した直後に引き継ぎとなりました。

1. **地図の構成要素の仕様**
   - ノードの種類：論文ノードだけか、「臨床疑問」「自分の結論」のような論文でないノードも置けるか（←アプリの性格を決める最重要論点。単なる引用図エディタか、エビデンス統合の思考ツールかの分かれ目）
   - 手動リンクに何を書けるか（ラベル、方向、種類など）
2. **自動リンクと手動リンクの視覚的区別**（線種・色など）
3. **Citrail との連携仕様**（インポートの形式＝JSON 等、受け渡し方法）
4. **技術構成と保存・共有**（ループ図アプリの流用範囲、保存方法）

## 4. 技術的な前提・参考情報

パスファインダー自体の技術構成は論点 4 で決めますが、姉妹アプリ Citrail の構成が参考になります：

- 静的サイト（HTML/JS/CSS のみ、ビルド不要）を GitHub Pages で公開
- データソースは OpenAlex API（利用者自身の API キーを登録する方式）、研究種別判定に PubMed（NCBI E-utilities）も使用
- 描画は Cytoscape.js（vendor/ にセルフホスト）
- お気に入り等の保存は Local Storage
- サーバー・アカウント機能・個人情報収集なし（公開リスクを構造的に回避する方針）

同じ方針（静的サイト＋GitHub Pages）を踏襲するのが自然ですが、パスファインダーは「編集して保存するエディタ」なので、保存・共有の設計は Citrail より重要になります（論点 4）。

## 5. 開発者（Yujin）について・進め方のルール

- Yujin は**非エンジニアの医師**（感染症内科）。コードは書きません。技術的な説明は、専門用語をかみ砕いた教育的な説明を心がけてください。
- やりとりは**日本語**で行ってください。
- **いきなりコードを書き始めないでください。** まず論点 1 から要件定義の議論を行い、仕様が固まった部分から実装に移ります。
- 変更はプルリクエスト（PR）として提案し、Yujin が内容を確認して承認（マージ）します。PR の説明文も日本語で、非エンジニアが読んで判断できるように書いてください。
- Citrail での協働スタイル：小さいバージョン刻みで実装 → Yujin が実際に動かして動作確認とフィードバック → 修正、の反復。細かい改修要望は TODO として溜め、まとまったところで一括改修することもあります。同じスタイルを踏襲してください。

## 6. 最初のセッションでやること

1. このファイルを読み、内容を理解したことを要約して伝える
2. 論点 1「地図の構成要素の仕様」の議論から再開する（ノードの種類の検討から）

---

## 7. 論点1〜4の決定事項（2026-07-23 セッションで確定）

4つの論点すべてについて、Yujinさんとの議論で仕様が固まりました。次回セッションはここから実装計画の相談、または実装そのものに進んでください。

### 論点1：地図の構成要素の仕様

**ノードの種類**（固定4種類。ユーザーが種類を自由追加する仕組みは不要）

| 種類 | 役割 | 個数制限 | 中身 |
|---|---|---|---|
| 論文ノード | 事実 | 複数可 | DOIは**任意入力**（空欄可）。DOI入力時はOpenAlexから書誌情報を自動取得し自動リンク対象になる。DOIがない場合（ガイドライン・教科書など）はタイトル等を手入力。加えて**自由記述メモ欄**（鍵となる知見・引用文など）を持つ |
| 臨床疑問ノード | 思考の起点 | **地図に1つだけ** | （内容の詳細仕様は未検討） |
| 自分の結論ノード | 思考の到達点 | **地図に1つだけ** | （内容の詳細仕様は未検討） |
| メモノート | 補助的な注釈 | 複数可 | 他ノードとリンクで接続可能（付箋止まりにしない） |

**手動リンク（人間の意味づけの線）**
- 種類：定型カテゴリ（支持する／反論する／補足する など）＋自由記述ラベルの併用
- 方向：一方向／双方向／無方向を線ごとに選択可能
- **同じノードペアに自動リンクと手動リンクが共存できる**（「事実」と「意味づけ」を重ねるという思想の核に合致）

### 論点2：自動リンクと手動リンクの視覚的区別

- **自動リンク（事実）＝点線・グレー（固定・中立色）**
- **手動リンク（意味づけ）＝実線・色はプリセットパレットから選択**（支持／反論／補足などのカテゴリに、用意された色候補からユーザーが割り当てる。緑＝支持／赤＝反論のような固定割当ではない）
- 決定理由：パスファインダーは「事実の記録」ではなく「自分の知見を固める」アプリなので、確定的な意味づけである手動リンクこそ実線（強い表現）、参考情報にすぎない自動リンクは点線（弱い表現）とする
- 同一ノードペアに自動＋手動が共存する場合：2本の線を少しカーブさせて並べて描画し、常に両方が見える状態にする

### 論点3：Citrailとの連携仕様

事前調査で判明した事実：このリポジトリ（`citation-history-map`）自体がCitrail本体であり、`https://yujin0722-lgtm.github.io/citation-history-map/` として公開される設計（`docs/publish-guide.md`）。パスファインダーも同じGitHubアカウントで公開すれば `https://yujin0722-lgtm.github.io/pathfinder/` となり、**両者は同一オリジン**（`yujin0722-lgtm.github.io`）になるため、Local Storageを共有できる。

- **選択の仕組み**：Citrailの既存「お気に入り」機能とは**別に**、ネットワーク図上にその場限りの「選択」チェックボックスを新設する（お気に入りは「後で読み返す用」、こちらは「今回の臨床疑問に関連する論文を選ぶ用」で目的が異なるため）
- **受け渡し方式**：同一オリジンのLocal Storage経由（URL埋め込みは不使用。サーバー不要でCitrailの「サーバー・アカウントなし」方針を維持できる）
  - キー名：`chm_to_pathfinder`
  - データ形式：選択した論文の配列（`id, title, authors, year, journal, doi, pmid`）のJSON
  - パスファインダー側は読み込み後にこのキーを消費（削除）する
- **受け取り時の挙動**：パスファインダーに未保存の作業中の地図がある状態で新規の論文が届いた場合、「新規に始めるか、今の地図に追加するか」を毎回確認する

### 論点4：技術構成と保存・共有

ループ図アプリ（`yujin0722-lgtm/loop-diagram-app`）を事前調査した結果、**Supabase（PostgreSQL＋認証＋リアルタイム通信）によるクラウド保存・メールログイン・リンク共有・共同編集機能**が組み込まれていることが判明。これはCitrailの「サーバー・アカウント機能・個人情報収集なし」という方針と矛盾するため、パスファインダーではこの部分は流用しないことで合意。

- **技術構成**：単一HTMLファイル（HTML/CSS/JS、ビルド不要）、GitHub Pages公開。描画はループ図アプリと同じSVG手描画方式を流用（外部ライブラリ不使用）。**Supabase関連のコード（ログイン・クラウド保存・リアルタイム共同編集）は持ち込まない**
- **保存**：サーバー・アカウントなし。「1地図＝1臨床疑問」という構造のため、Local Storageに複数地図を一覧管理する「地図一覧」機能を新設。編集のたびに**自動保存**（手動保存ボタンは不要）
- **共有**：JSONファイルの書き出し／読み込みで手渡し（ループ図アプリのJSON書き出し機能をベースに流用）

## 8. 論点1の補足決定（2026-07-23 セッション、続き）

### 臨床疑問ノード・自分の結論ノードの中身

- 臨床疑問ノード：**自由記述のみ**（PICO形式などの構造化入力は導入しない）
- 自分の結論ノード：自由記述

### ノードの見た目（最終版）

| 種類 | 形 | 色 |
|---|---|---|
| 論文ノード | **研究種別によって変化**（Citrailの分類ロジックをそのまま流用：PMIDがあればPubMedの公式Publication Typeで判定、なければタイトルからの暫定判定）。RCT＝ひし形／メタ解析・SR＝六角形／レビュー＝角丸長方形／観察研究＝三角形／ガイドライン＝星形／症例報告＝タグ形／その他・未判定＝楕円 | 白（固定） |
| 臨床疑問ノード | 二重枠の長方形 | 水色 |
| 自分の結論ノード | 二重枠の長方形 | 緑 |
| メモノート | 付箋風（角が折れた四角） | 黄色 |

補足：当初「臨床疑問＝六角形」を検討したが、論文ノードの「メタ解析・SR＝六角形」と衝突するため、臨床疑問・自分の結論は論文が使わない「二重枠の長方形」に変更した（両者は色（水色／緑）で区別）。

### 実装しながら判断してよいとされた細部（未決定・要implementer判断）

以下は仕様上重要ではあるが、Yujinさんの了承のもと「実装しながら判断してよい」項目として保留された。実装中に判断に迷った場合、勝手に決めずメイン会話に差し戻すこと。

1. 臨床疑問／自分の結論ノードの「地図に1つだけ」制約のUI上の運用（追加ボタンを無効化するか、上書き確認を出すか等）
2. Local Storageで複数地図を一覧管理する「地図一覧」における、各地図の名前の付け方（臨床疑問ノードの文章をそのまま使うか、別途タイトルを付けるか）
3. 手動リンクの定型カテゴリ（支持する／反論する／補足する）が固定3種類か、ユーザーが自分でカテゴリを追加できるようにするか

## 9. 実装の進捗（2026-07-23 セッション）

`index.html` として実装を開始した。実装済みの範囲：

- v0.1：4種類のノード（論文・臨床疑問・自分の結論・メモ）の配置・移動・編集、手動リンクの作成・編集・削除、元に戻す/やり直し、Local Storageへの自動保存、JSON書き出し/読み込み
- 不具合修正：リンク削除ボタンの配線漏れ、研究種別ごとの図形が横長に潰れていた見た目の修正
- DOI入力・OpenAlex自動取得（書誌情報・研究種別）・PubMed Publication Typeによる研究種別の上書き・OpenAlexの引用関係に基づく自動リンク生成（点線・グレー、手動編集不可）・自動/手動リンク共存時の並行カーブ描画

- 複数地図の一覧管理：Local Storageに複数の地図を保存し、トップバーの「地図一覧」から一覧・切り替え・改名・削除ができるようにした。「新しい地図」は今の地図を消さず追加する形に変更。v0.1の単一地図データは自動移行
- Citrailとの連携：Citrail側（`citation-history-map`リポジトリ）のネットワーク図に「📌 パスファインダーへ送る」チェックボックスと送信ボタンを追加。同一オリジンのLocal Storage（キー：`chm_to_pathfinder`）経由で、書誌情報に加えて研究種別・引用関係もまとめて渡すようにしたため、パスファインダー側で受け取った直後から形・自動リンクが正しく表示される
  - その後の改善：送信は新しいタブでパスファインダーを開く方式に変更／同じ論文（DOI・PMID・OpenAlex IDのいずれかが一致）が重複して送られてもノードが重複しないようにした／送信時にCitrail側で送り先の地図（既存の地図一覧から選ぶ、または新規作成）を指定できるようにし、パスファインダー側は指定にそのまま従う形にしたため、以前あった「今の地図に追加／新規」の確認ダイアログは廃止した

論点1〜4で計画していた実装項目はこれで一通り完了。次はセッション11に記録した「外観・UI・操作性の改善TODO」に取り組む段階。

## 10. 将来のアイデア（試験的実装の候補、未着手）

Yujinさんから、今すぐの実装は不要だが記憶しておいてほしいと依頼されたアイデア。

### ノードドラッグ時の「ゴムのような」連動UI

- vis.jsのネットワーク図のように、1つのノードをドラッグすると、つながっている他のノードもバネで引っ張られるように連動して動くUIにできないか、という提案
- 技術的には実現可能。力学シミュレーション（リンクをバネ、ノード同士に反発力を持たせる）で実装する
- ただし常時の物理演算（vis.js的な「整列モード」）は、力の調整を誤るとノードが震え続けたり画面外に弾き飛ばされたりする不具合が起きやすく、アニメーションループの新設や自動保存・元に戻す機能との共存など、実装・検証コストが高い
- 一方、「ドラッグしている間だけ、直接つながっている隣接ノードが緩やかに引っ張られ、手を離したらそこで位置が固定される」という限定版は、既存のドラッグ処理（mousemoveのたびに位置を再計算する仕組み）にそのまま乗せられ、状態もその場限りで残らないため、技術的なエラーが起きにくい
- **次に着手するとしたら、まずは「ドラッグ中だけ隣接ノードが引っ張られる」限定版から試すのが安全**、という結論をYujinさんと話した
- なお、パスファインダーの思想（ノードの位置も含めて自分で意味づけて配置する地図）を踏まえると、常時物理演算で位置が自動的にずれ続ける挙動は、この思想と衝突する可能性がある点も議論した

## 11. 外観・UI・操作性の改善TODO（2026-07-23 セッション、Citrail連携完了後に着手・詳細を確認済み）

Yujinさんから挙げられた改善案。Citrail連携（論点1〜4）が完了したことを受けて、着手前に一つずつ詳細を確認した。**このセッションでは実装していない**（設計方針の確認のみ）。次にUI改善の実装に着手する回は、以下の確定仕様に沿って進めること。

1. **全体の見た目をCitrailに近づける**：ただし配色は完全に同一にはせず、**同じ系統だが少し違う色**にする（パスファインダーがCitrailから派生した別アプリだと一目で分かるように、という理由）。背景・文字色・枠線などの基調はCitrailの配色変数（`--canvas:#F3F6F8` `--panel:#FFFFFF` `--ink:#1C2B33` `--sub:#5B6B77` `--line:#DDE5EA`）に揃えつつ、差し色（Citrailは `--accent:#0E6E6E` のティール系）だけ、同系統でわかりやすく違う色に変更する（実装時に具体的な色を提案し画面で確認する）
2. **臨床疑問をヘッダーに表示**：地図名の入力欄は廃止し、臨床疑問ノードの内容をそのままヘッダーに表示する（地図一覧でもこの内容を名前として使う）。臨床疑問ノードが未設定の地図では「（臨床疑問未設定）」のような表示にする
3. **ノードの大きさを変更できるようにする**：選択中のノードの角にドラッグハンドルを表示し、ドラッグでリサイズする方式
4. **ノードに表示される名前が短すぎる**：3のリサイズとあわせて、ノードの大きさに応じてラベルの表示量（折り返し・文字数）を変える
5. **論文ノードの「研究種別」テキスト表示は不要**：形状で判別できるため、ノード下のサブテキスト（例：「RCT」「観察研究」）表示は削除する
6. **「論文」という言葉を「エビデンス」に置き換える**：論文ノードはDOIのない情報源（ガイドライン等）も扱うため、「論文」より「エビデンス」の呼び方が実態に合う。ボタン・見出し等の表示文言を置き換える（内部のデータ上の種類名`nodeType:'paper'`は変更不要）
7. **出版年に基づく時間軸整列**：ボタンを押したときに一度だけ、出版年を軸にノードを並べ替える機能（常時動く物理演算ではない）。**同じ出版年のノードが重ならないよう、縦方向にずらして整理する**。発表年情報がないノード（臨床疑問・自分の結論・メモ、および年未入力の論文ノード）は整列の対象外とし、今の位置のまま動かさない
8. **背景にグリッド線を表示したい**：ループ図アプリには元々あった機能（`gridBg`）。パスファインダーには未移植なので追加する（切り替え可能に）
9. **左のツールバーのアイコンを改善したい**：1.の見た目の統一とあわせて、Citrail流（絵文字・記号＋短い文字ラベル）に寄せる方向で見た目を作り直す
10. **右側のプロパティパネルを開閉できるようにしたい**：Citrailの`#panel-toggle`と同様、常時表示ではなく開閉トグルを追加する
11. **全画面表示に対応する**：ブラウザの正式なFullscreen APIではなく、**Citrailと同じ方式**（CSSでヘッダー・ツールバー等を隠してキャンバス領域を広げる疑似フルスクリーン）にする
12. **（今回追加）PNGで保存する機能をつける**：ループ図アプリ・Citrailにあった「画像として書き出す」機能がパスファインダーにはまだない。地図をPNG画像として書き出せるようにする

## 12. 実装完了（2026-07-23 セッション、続き）

上記「11. 外観・UI・操作性の改善TODO」の1〜12すべてを実装した。主な実装内容：

- 配色：`--accent`をCitrailのティール系(`#0E6E6E`)から、同系統だが違うブルー系(`#1B5FA6`)に変更。他の配色変数(`--canvas` `--panel` `--ink` `--sub` `--line`)はCitrailと共通
- ヘッダー：地図名の入力欄を廃止し、臨床疑問ノードの内容を表示。地図一覧のインライン改名機能も、名前が疑問ノード由来になったため削除した
- ノードリサイズ・ラベル折り返しを実装
- 研究種別テキストの削除、「論文」→「エビデンス」の表示文言変更
- 時間軸整列（同年ノードは縦にずらして重ならないようにする）
- グリッド線：**必ずノード・リンクより背面に描画し、`pointer-events:none`を設定**（過去にループ図アプリで発生した「グリッドが前面に出て選択できなくなる」不具合の再発防止）
- プロパティパネルの開閉トグル
- 全画面表示（Citrailと同じ疑似フルスクリーン）：実装中に「全画面ボタン自体がヘッダーごと隠れて抜け出せなくなる」不具合に気づき、抜け出す専用ボタン（常にキャンバス上に浮く）とEscキーの両方を用意して対応した
- PNG書き出し機能

ブラウザ(Chromium)で全項目の動作確認と、グリッド表示中のノード選択・全画面からの脱出を含む重点確認、既存機能への回帰確認を実施済み。PR #11は既にマージ済み。

## 13. 追加の改修TODO（2026-07-23 セッション、実際に使ってみてのフィードバック。詳細確認済み・未実装）

Yujinさんが実際に動かしてみてのフィードバック。**このセッションではまだ実装していない**。次回、以下の確定仕様に沿って実装する。

1. **Citrail→パスファインダー送信時に自動で年代整列**：受け取り時（`importPapersFromCitrail`後）に`layoutByYear()`相当を自動実行する
2. **PMID・PubMedリンクの自動付与**：エビデンスノードにPMIDが取得できている場合、PMIDを表示し「PubMedを開く」リンク（`https://pubmed.ncbi.nlm.nih.gov/{pmid}/`）を追加する
3. **ノード情報パネルのデザインをCitrailに近づける**：Citrailの詳細パネル（タグ・見出し・項目レイアウトなど）に寄せる。※画像にあった「IF: 109.0」「Q1」バッジはYujinさんのブラウザ拡張機能によるものでCitrail本体の機能ではないため、再現しない
4. **出版年をノードの隅に表示**：出版年があるエビデンスノードは、ノードの左上か右上に年を表示する
5. **ノード位置の固定機能**：固定したノードはドラッグ・自動整列（年代整列など）の対象外にする
6. **ノード色をプリセットから変更できる機能**：手動リンクの色選択と同様、プリセットパレットから選ぶ方式
7. **ノードの名前とメモの表示分離（エビデンスノードのみ）**：ノードの「名前」（タイトル）はノードの外側・下に表示（30字程度、Citrail風）。ノードの中には「メモ」欄の冒頭10字程度を表示する。臨床疑問・自分の結論・メモノートは対象外で、現状通りノード内に中身をそのまま表示する
8. **クリックでメモを展開表示**：エビデンスノードをクリックすると、ノードの右上あたりにメモの内容が展開表示される。折りたたむボタンを押すまで表示され続ける
9. **【不具合修正】改行を含む内容が地図上で消える**：メモノート・臨床疑問ノード・自分の結論ノードで、内容に改行が含まれると、地図上の表示は1行目までしか出ない（`nodeLabelText()`が`split('\n')[0]`で先頭行のみを取っているのが原因）。折り返し表示の対象に改行後の内容も含めるよう修正する
10. **手動リンクの「種類」選択を廃止**：定型カテゴリ（支持する／反論する／補足する）の選択肢とその仕組み自体を削除する。自由記述ラベルと色（プリセットパレットから選択）は残す
11. **手動リンクの色をパステルカラーに変更**：現在の濃い色のプリセットを、パステル調の色に置き換える
12. **【不具合修正】自分の結論ノードで矢印が埋まる**：結論ノードに矢印が重なって内容が見えにくくなる問題を修正する

## 14. 実装完了（2026-07-23 セッション、追加の改修TODO 1〜12）

上記「13. 追加の改修TODO」の1〜12すべてを実装した。実装前に確認した詳細仕様：

- 1（自動年代整列）：Citrailから取り込んだ**新規ノードのみ**を対象にする（既存ノードは動かさない）
- 5（位置固定）：プロパティパネルのチェックボックスで切り替え。ドラッグと年代整列のみ対象外にし、リサイズは制限しない
- 6（色プリセット）：4種類のノードすべてに適用
- 8（メモ展開）：複数ノードを同時に展開可能。それぞれ個別の「折りたたむ」ボタンで閉じる
- バッジの配置：出版年バッジ＝ノード左上、メモ展開の吹き出し＝ノード右上寄りに表示（衝突しないように）
- メモが空の場合の表示：ノード内の抜粋・展開表示ともに、タイトルの抜粋にフォールバックする

主な実装内容：

- `layoutNewImportedNodes()`を新設し、`importPapersFromCitrail()`から呼び出し。取り込み前の地図の内容の下（発表年が2件以上ある場合のみ）に配置し、既存内容とは重ならないようにした
- エビデンスノードのプロパティパネルに、PMID表示と「PubMedを開く」リンクを追加
- プロパティパネルをCitrailの詳細パネル（タグ＋タイトル＋dl形式の項目レイアウト）に寄せて再構成（`.p-top` `.p-tag-select` `.p-title-input` `.node-dl`）。IF/Q1バッジは再現していない
- エビデンスノードに出版年バッジ（左上）を追加
- 全ノード種別に「位置を固定する」チェックボックスを追加。ドラッグ(`dragging.locked`)と`layoutByYear`/`layoutNewImportedNodes`の対象から除外
- 全ノード種別に色プリセットの選択UI（`NODE_COLOR_PRESETS`）を追加
- エビデンスノードのみ、タイトルをノード外側・下に表示（Citrail風、約30文字）し、ノード内にはメモ冒頭10文字程度（空ならタイトル冒頭）を表示するよう分離
- エビデンスノードをクリック（ドラッグを伴わない）するとノード右上寄りにメモを展開表示。複数ノードを同時展開可能、それぞれ独立して折りたたみ可能。ドラッグ操作と誤判定しないよう、マウスダウン位置からの移動量で「クリックか、ドラッグか」を判定するようにした
- `nodeLabelText()`/`wrapLabelLines()`のバグを修正：改行(`\n`)を強制的な行区切りとして扱い、折り返し可能な範囲で改行後の内容も表示するようにした（以前は`split('\n')[0]`で先頭行のみ表示していた）
- 手動リンクの「種類」選択（`EDGE_CATEGORIES`等）を完全に削除。自由記述ラベルと色選択は維持
- 手動リンクの色プリセットをパステル調に変更
- `nodeBoundaryPoint()`を修正：臨床疑問・自分の結論・メモノートは長方形との交点を厳密に計算するようにし、矢印が中に埋まる不具合を解消（それ以外のエビデンスノードの形は従来通り楕円近似のまま）

ブラウザ(Chromium)で全項目の動作確認（改行を含むテキストの折り返し、ノード色変更、位置固定によるドラッグ・年代整列の除外、メモ展開/折りたたみ、Citrailからの取り込み時の新規ノードのみ年代整列、手動リンクのパステル色、結論ノードでの矢印の交点が長方形の境界線上に正しく乗ることなど）と、既存機能（DOI取得・自動リンク・地図一覧・削除・リサイズ・PNG書き出し・Citrail連携の新規タブ/重複防止/送り先指定）への回帰確認を実施済み。

## 15. 実装完了（2026-07-23 セッション、続き。PR#12レビューコメントの修正 + 新規フィードバック6件）

PR#12のレビューコメントで、研究種別「レビュー」のエビデンスノードも角丸長方形なのに矢印の交点計算が楕円近似のままだった不具合を指摘され、`nodeBoundaryPoint()`の対象に`studyType==='REVIEW'`のエビデンスノードを追加して修正した（PR#13としてマージ済み）。

続けてYujinさんから新しいフィードバック6件があり、実装した：

1. **被引用数の取得・表示**：OpenAlexの`cited_by_count`を`OPENALEX_SELECT`に追加し、DOI取得時に`node.cites`へ保存。詳細パネルのdl一覧に「被引用数」の行を追加
2. **ノード内表示の優先順位変更**：「PMID→メモ冒頭→タイトル冒頭」の順に変更（`nodeInsideExcerpt()`）。PMIDがあれば`PMID 12345678`のように表示し、メモの有無に関わらず最優先
3. **研究種別タグとタイトル入力欄を別の行に**：`.p-top`を横並び(flex row)から縦並び(flex column)に変更し、タイトル入力欄がパネル幅いっぱいに表示されるようにした
4. **メモ展開ウィンドウをドラッグでリサイズ可能に**：ノード本体のリサイズと同様の考え方で、展開ウィンドウ右下に専用のリサイズハンドルを追加(`resizingMemoBox`状態、`expandedMemoSizes`は一時的な状態としてノードごとに保持、地図データには保存しない)
5. **展開を閉じるボタンを「×」のシンプルなアイコンに変更**
6. **ノード色プリセットを6色×2段階(明るい/濃い)の12色に整理**：`NODE_COLOR_PRESETS`を8色から12色(6列×2行、既存の`swatch-row`グリッドがそのまま流用できる並び)に変更。この設計はYujinさんに選択肢を提示して決定してもらった（6色×3段階、6色+スライダー、6色×2段階、の3択から6色×2段階を選択）

ブラウザ(Chromium)で全項目の動作確認（被引用数の表示、PMID優先表示、タグ/タイトルの行分割、メモ展開ウィンドウのドラッグリサイズ、×ボタンでの折りたたみ、12色パレットの表示）と、既存機能への回帰確認（DOI取得・地図一覧・削除・リサイズ・PNG書き出し・Citrail連携全般・前回実装分の年代整列/位置固定/矢印境界計算など）を実施済み。

## 16. 追加の改修TODO（2026-07-24 セッション、提案のみ・未実装）

Yujinさんからの改善提案。**まだ実装していない**。実装前に必ず内容の確認を行うこと（Yujinさんのルール：「改善点の提案です」と言われた場合、コーディングを開始する前に必ず確認する）。

1. ノードに表示される文字の大きさを変更できる機能
2. ノード図右下の操作説明（`#hint`）の表示・非表示を切り替えるボタン

## 17. 実装完了（2026-07-24 セッション、続き。小さな改修2件）

Yujinさんから「改修点」として直接依頼があった、仕様が明確な小さな修正2件を実装した（「改善点の提案です」ではなく直接の依頼だったため、確認なしでそのまま実装）：

- 出版年バッジの文字サイズを10px→12px（太字）に、バッジの箱も少し拡大
- ノード詳細パネルの色プリセットの下に「白に戻す」ボタンを追加し、`node.color`を`#ffffff`に戻せるようにした

## 18. 実装完了（2026-07-24 セッション、続き。TODO2件 + 新規改修3件）

「16. 追加の改修TODO」の2件と、Yujinさんから新たに直接依頼された3件（「改善点の提案です」ではなく「コーディングを開始し、PRまで作成してください」という明確な実装指示だったため、確認なしでそのまま実装）を実装した：

1. **ノードに表示される文字の大きさを変更できる機能**：ツールバーに「文字小」「文字大」ボタンを追加。`labelFontSize`(既定12px、9〜20pxの範囲、`pf_label_font_size`としてLocal Storageに保存)を`.node-label`のCSS変数`--label-font-size`に反映し、`wrapLabelLines()`の折り返し計算(1文字あたりの幅・行の高さ)も同じ比率で拡縮するようにした。PNG書き出し時のインラインスタイルにも現在の文字サイズを反映
2. **操作説明(`#hint`)の表示・非表示ボタン**：ツールバーに「ヒント」ボタンを追加し、グリッド表示と同様に`pf_hint_visible`としてLocal Storageに好みを保存
3. **Citrail風の凡例(表示・非表示可能)**：Citrailの凡例と同じ位置(キャンバス左上)・同じ折りたたみ方式(見出しクリックで中身だけ畳む「▾/▸」+ ツールバーボタンで全体の表示/非表示)を実装。中身はパスファインダー用に、研究種別の形の凡例(Citrailと同じ7形状)、ノードの種類(❓臨床疑問/✅結論/📝メモ)、線の種類(自動リンク＝点線、手動リンク＝実線)、その他の注記をまとめた。表示/非表示は`pf_legend_visible`としてLocal Storageに保存
4. **グリッド線を少し濃く**：`#DCE2E7`→`#C3CDD5`に変更
5. **臨床疑問・結論ノードを色以外でも区別できるように**：ノード左上に、ツールバーの追加ボタンと同じアイコン(❓＝臨床疑問、✅＝結論)を表示するようにした

ブラウザ(Chromium)で全項目の動作確認(文字サイズの拡大縮小と保存の永続化、ヒント/凡例のトグルと折りたたみとLocal Storageへの保存、グリッド線の色、臨床疑問・結論ノードのアイコン表示)と、既存機能全般への回帰確認を実施済み。

## 19. 追加の改修TODO（2026-07-24 セッション、続き。提案のみ・未実装）

Yujinさんからの改善提案。**まだ実装していない**。実装前に必ず内容の確認を行うこと。

1. **仕様確定**：自動リンク(OpenAlexの引用関係)を表示/非表示できるようにする。設定はグリッド・ヒントと同じ方式(ブラウザ全体の好みとしてLocal Storageに保存、地図ごとではない)
2. **仕様確定**：左のツールバーからアイコンを削除し、バー(ツールバー)の長さを短くする。文字ラベルは分かりやすく保つ
3. ノードごとに文字サイズを変える機能（`node.fontSize`のようなノード単位のプロパティを追加し、プロパティパネルに設定UIを設ける）。**仕様確定**：これを実装する際は、地図全体で共通のツールバーの文字サイズ変更機能(「文字小」「文字大」ボタン、`labelFontSize`)は削除し、ノードごとの設定に置き換える。UIは既存の「文字小」「文字大」ボタンと同じ+/-方式でよい
4. 【仕様確定・「9.」と合わせて実装】エビデンスノードのメモ展開のトリガーを、現在の「ワンクリック」から「ダブルクリック」に変更する
5. 手動リンクの矢印を太くしてほしい
6. **仕様確定**：ヒント(`#hint`)の内容が表示されない不具合は、起動時に`updateHint()`を一度呼ぶ形で修正する(ヒント自体は削除しない)
7. **仕様確定**：結論ノードの形は、案C「二重の細い枠(疑問ノードと同じ)ではなく、太い単線の枠にする」を採用する
8. **仕様確定**：ノードに被引用数があるときは、ノードの**左下**に表示する(右下はノード選択時のリサイズハンドルと重なるため)。これに伴い、固定バッジ(🔒、これまで左下)は**右上**に移す(出版年バッジは左上のままで重ならない)
9. **仕様確定**：エビデンスノードのみを対象に、「ワンクリック＝画面右の詳細パネルの表示切替、ダブルクリック＝メモの展開表示切替」という操作にする。臨床疑問・自分の結論・メモノートは対象外で、現状通り(クリックで選択→詳細パネルに表示)のまま

## 20. 実装完了（2026-07-24 セッション、続き。年代整列の縦方向ルール明確化）

Yujinさんから、年代整列(`layoutByYear`)の縦方向の並び順が不明瞭だという相談があった。「被引用数をノードの重さと捉え、横軸=年代・縦軸=被引用数(上が軽い＝少ない、下が重い＝多い)の2軸整列にしてはどうか」という提案に対し、比例配置(値をそのままY座標に反映)だとスケールの偏り・欠損値の扱いが難しいことを説明し、代わりに「同じ年のグループ内で被引用数の順に並べ替えて、既存の固定間隔(vGap)で上から下に積む」という順位ベースの方式を提案したところ、Yujinさんに合意いただいた。

実装内容：
- `layoutByYear()`と`layoutNewImportedNodes()`の両方で、同じ年のグループ内のノードを並べる前に`(a.cites||0) - (b.cites||0)`で昇順ソートするようにした(被引用数未取得のノードは0として扱う)
- Y座標自体は被引用数の値に比例させず、あくまで順序のみを反映する（実際の量の差はノードの太さ等では表現していない、目安の整列という位置づけ）

Playwrightで、被引用数の異なる複数のエビデンスノードを同じ年に配置し、年代整列後の縦位置の並び順が被引用数の昇順と一致することを確認済み。既存機能への回帰確認も実施済み。

## 21. 実装完了（2026-07-24 セッション、続き。年代整列の原点指定機能）

Yujinさんから、「ノードが増えると年代整列の結果が予想しづらい」という相談があり、「原点(ゼロ点)を指定できるようにし、その点から右下方向に整列させたい。自動整列されないノード(固定ノードなど)との兼ね合いを考えて操作しやすくしたいから」という要望があった。合意した仕様どおりに実装した。

- 「年代整列」ボタンを押すと、レイアウトがすぐには実行されず、`pick-year-origin`という専用モードに入る(カーソルが十字に変化し、下部のヒントに「整列の基準にしたい位置をクリックしてください」と表示される)
- キャンバス上の空白をクリックするとその点が原点(左上)になり、既存ノード(固定ノードなど)を直接クリックするとそのノードの左上座標がそのまま原点になる
- 原点から右下方向に、年代(横軸)×被引用数昇順(縦軸、既存の「20.」の仕様のまま)でエビデンスノードを配置する
- 対象ノード(発表年入り・位置固定されていないエビデンス)が2件未満の場合は、今まで通り原点指定モードに入る前にアラートで案内する
- Citrailから取り込んだ新規ノードのみを対象にする自動整列(`layoutNewImportedNodes`、既存ノードの下に自動配置)は今回の変更対象外で、従来通り

Playwrightで、原点指定モードへの遷移・空白クリックでの原点設定・既存ノード(固定ノード含む)クリックでの原点設定・年代×被引用数の配置結果を確認済み。既存の回帰テスト群(この機能を使うテストも含む)も、新しい2段階操作に合わせて更新し、全て通過を確認済み。

## 22. 実装完了（2026-07-24 セッション、続き。年代整列を「年代ごとの順位」から「全体での順位」に修正）

PR #16マージ後、Yujinさんから実際の画面のスクリーンショット2枚を使ったフィードバックがあった。「21.」で実装した仕様には誤解があり、当初想定していたのは「①違う年代のノード同士でも被引用数の重さの差が見えるようにする」「②リンクが重ならないよう、なるべく同じ高さ(横軸)にノードが並ばないようにする」という2点だったが、実装は「同じ年の中だけ」で被引用数を比較・整列していたため、年をまたいだ重さの比較ができておらず、また同じ年の中の順位がそのまま同じ高さのバンドを作ってしまい、結果としてリンクが集中する箇所ができていた。

修正内容：
- `runLayoutByYearFromOrigin()`(手動の「年代整列」)と`layoutNewImportedNodes()`(Citrail取込時の自動整列)の両方で、「同じ年の中だけ」でソートするのをやめ、**年に関わらず対象ノード全体**を被引用数の昇順で1回ソートし、その全体順位(0,1,2...)をそのままY座標の段(`vGap`間隔)に割り当てるように変更した
- X座標は従来通り年代で決まる(横軸=年代の考え方は維持)
- 全体順位でY座標を割り当てることで、①年をまたいだ重さの比較が視覚的に分かるようになり、②各ノードが基本的に別々の高さになるため、②の「同じ高さに集まって線が重なる」問題も自然に解消される(追加の水平方向のジグザグ処理などは不要だった)

Playwrightで、複数の年代・被引用数を持つノード群を用意し、「Y座標の並び順」と「被引用数の昇順」が年をまたいで完全に一致することを確認済み。X座標が年代順に単調増加することも確認済み。既存の回帰テスト群も全て通過を確認済み(一部の古いテストのコメントは旧仕様(年ごとのグループ化)を前提にした記述のままだが、動作自体に問題はない)。

## 23. 実装完了（2026-07-24 セッション、続き。Citrail→パスファインダー連携で被引用数が抜けていた不具合を修正）

Yujinさんから、「Citrailからパスファインダーに送る」を使うと被引用数が取得されない(古い仕様の問題ではないか)という指摘があった。原因は、被引用数(`cites`)を取得・表示する機能(「20.」)がCitrail→パスファインダーの受け渡し(`chm_to_pathfinder`のペイロード)を設計した後に追加されたため、送信側(Citrail)のペイロード生成コードに`cites`フィールドが単純に含まれていなかったこと。両リポジトリを修正した：

- Citrail側(`panels.js`の`sendToPathfinder()`)：送信するpapers配列の各要素に`cites: p.cites`を追加
- パスファインダー側(`index.html`の`importPapersFromCitrail()`)：受け取った`cp.cites`を`node.cites`に反映するようにした

Playwrightで、Citrailで被引用数を取得済みの論文をパスファインダーへ送信し、受け取った側のノードデータとプロパティパネルの「被引用数」欄に正しく反映されることを確認済み。既存のCitrail連携の回帰テスト(新規タブ表示・送り先地図指定・重複防止など)も全て通過を確認済み。

## 24. 実装完了（2026-07-24 セッション、続き。「19. 追加の改修TODO」9件をすべて実装）

「19.」に記録していた9件のTODO(仕様確定済み)をすべて実装した。

1. **自動リンクの表示/非表示**：ツールバーに「自動リンク」ボタンを追加。グリッド・ヒントと同じ方式で`pf_autolink_visible`をLocal Storageに保存(ブラウザ全体の好み、地図ごとではない)。非表示時は`render()`/`renderQuiet()`で`edge.auto`なリンクの描画自体をスキップする
2. **ツールバーのアイコン削除**：全ボタンから`<span class="ic">`を削除し、テキストラベルのみに。ボタンのレイアウトを`flex-direction:column`(アイコン+文字を縦積み)から単純な1行テキストに変更し、ツールバー幅・パディングも詰めて全体を短くした
3. **ノードごとの文字サイズ変更機能**：`node.fontSize`(既定12、9〜20pxの範囲)を追加し、プロパティパネルに「文字小/文字大」の+/-ボタンを設置(全4種類のノードタイプ共通)。これに伴い、地図全体で共通だったツールバーの文字サイズボタン(`labelFontSize`、`btn-fontsize-dec/inc`)は完全に削除した。`wrapLabelLines()`の折り返し計算・`renderNode()`の行間も`node.fontSize`基準に変更
4. **(9と統合実装)** エビデンスノードのメモ展開トリガーをダブルクリックに変更
5. **手動リンクの矢印を太く**：手動リンク専用の大きめの矢印マーカー(`#arrow-manual`)と`stroke-width:3`(選択時4)の`.manual`クラスを追加。自動リンクは従来通り
6. **ヒント不具合の修正**：起動時に`updateHint()`を一度呼ぶようにし、ツールボタンを一度も押さないまま操作しても空欄にならないようにした
7. **結論ノードの形状変更**：疑問ノードは従来通り二重の細い枠のまま、結論ノードは内側の枠線を廃止し、太い単線の枠(`stroke-width:4`、選択時5)に変更した
8. **被引用数バッジ・固定バッジの配置**：被引用数バッジをノード左下に追加(出版年バッジと同じ見た目)。固定バッジ(🔒)は左下から右上に移動し、被引用数バッジとの衝突を回避した
9. **エビデンスノードのクリック挙動変更**：ワンクリック(ドラッグなし)で画面右の詳細パネルの表示/非表示を切り替え、ダブルクリックでメモの展開/折りたたみを切り替えるようにした。ダブルクリック判定は300ms以内の同一ノードへの再クリックで行い(`pendingNodeClick`)、ドラッグや別ノードへのクリックでは正しくキャンセルされる。臨床疑問・自分の結論・メモノートは対象外で、これまで通りクリックで選択→即座にパネルに表示される

ブラウザ(Chromium)で全項目を個別に動作確認した上で、既存の回帰テスト一式(22本)も全て通過することを確認済み。特にクリック/ダブルクリックの新しい判定ロジックは、シングルクリック単体・ダブルクリック・ドラッグ・別ノードへの切り替え・ロックされたノードでの挙動など、想定される組み合わせを個別にテストして確認した。なお、この変更に伴い既存の一部テスト(`ui-improvements-test.js`のパネル開閉テスト、`round2-test.js`・`round3-test.js`のメモ展開テスト、`round5-test.js`の文字サイズテスト)を新しい仕様に合わせて更新した。

## 25. 追加の改修TODO（2026-07-25 セッション、提案のみ・未実装）

Yujinさんからの改善提案。**まだ実装していない**。実装前に必ず内容の確認を行うこと。

1. ノード上のPMID表示から「PMID」という文字を外し、番号のみにする(`nodeInsideExcerpt()`の`'PMID ' + node.pmid`を`node.pmid`のみに変更すればよい)
2. 左ツールバーの文字表示を変更する：「エビデンス追加」→「ノード」、「疑問を追加」→「疑問」、「結論を追加」→「結論」、「メモ追加」→「メモ」、「自動リンク」→「引用関係」、「年代整列」→「自動整列」(それ以外のボタン名は変更しない)
3. 図(キャンバス)の何もない場所をクリックすると、右側の詳細パネルが閉じるようにする(現状は選択解除のみで、パネルの開閉状態は変わらない。`svg`の`mousedown`ハンドラの`tool==='select'`時の`selectItem(null,null)`の処理に、`panelWrapEl`を閉じる処理を追加すればよい)
4. ノード詳細パネルの「位置を固定する」チェックボックスのアイコンを、現在の📌(押しピン)から別のものに変更する。📌はCitrail側で「パスファインダーへ送る対象」という別の意味で使っており紛らわしいため。ノード上の固定バッジは既に🔒を使っているので、パネル側のラベルも🔒に統一するのが自然(`lockRowHtml()`の📌を🔒に変更すればよい)

**まだ実装していない**。次のセッションで、実装前にYujinさんに内容を確認すること。

## 26. 次回セッションでやること

- 「25. 追加の改修TODO」について、実装に入る前に必ずYujinさんに内容を確認する
- 確認後、合意が取れた仕様のみ実装する

## 27. DOI/PMID対応の拡張・複数追加機能（2026-07-25 セッション、実装完了）

前回セッションで構想だけ合意していた内容(Citrailと同様にDOIだけでなくPMIDも受け付ける、複数のエビデンスをまとめて追加する機能)を、セッションリセット後に実装した。

**実装内容**：
1. ノード詳細パネルの「DOI(任意)」欄を「DOIまたはPMID(任意)」に変更。CitrailのnormalizeInput()と同じ判定ロジックを`normalizeDoiOrPmid()`として移植し、DOI URL・`doi:`接頭辞・裸のDOI文字列・PubMed URL・`pmid:`接頭辞・裸の数字(1〜9桁)のいずれの形式でも入力を受け付ける。取得APIのパスは判定結果に応じて`/works/doi:{doi}`または`/works/pmid:{pmid}`を使い分ける(`identifierApiPath()`)。既存の`fetchAndApplyDoi()`は`fetchAndApplyIdentifier()`に改名し、この判定ロジックを使うよう変更
2. 新しい「複数追加」機能：ツールバーに「複数追加」ボタンを追加し、モーダル(`#batchAddModal`)を開いてDOI/PMIDを1行に1件ずつ貼り付けられるテキストエリアを表示。「取得して追加」ボタンで各行を順番に(直列で)OpenAlexへ問い合わせ、取得できた分をエビデンスノードとして一括作成する。判定ロジックは上記の`normalizeDoiOrPmid()`を共有。既存の重複判定(`findExistingPaperNode()`、DOI・PMID・OpenAlex IDのいずれか一致)・自動整列(`layoutNewImportedNodes()`)・自動リンク再構築(`recomputeAutoLinks()`)の仕組みをそのまま再利用。認識できない行や取得失敗した行はスキップしてエラー内容を一覧表示し、成功した件数・重複でスキップした件数と合わせてモーダル内に結果表示する

Playwrightで、単体のDOI取得欄でのDOI/裸PMID/`pmid:`接頭辞それぞれでの取得・複数追加モーダルでのDOI+PMID混在入力・認識できない行のエラー表示・重複時のスキップ表示、を確認済み。既存のDOI取得回帰テスト(`doi-test.js`：自動リンク生成・PubMed分類上書き・手動リンク併記)も引き続き通過することを確認済み。

## 28. 原点・軸表示アノテーション機能（2026-07-26 セッション、仕様確定・未実装）

Yujinさんが手書きで画像に描き込んで説明した機能。

**やりたいこと**：地図上に「原点＋軸の向き」を示す注釈を置けるようにしたい。具体的には、L字型に伸びる矢印(右向き矢印＝「Year」ラベル、下向き矢印＝「Citation」ラベル)を、原点となる1点から伸ばした図形。「年代整列」機能でノードを整列する際の基準(原点は左上、右方向が発表年、下方向が被引用数が多い方向)を、見た目としてキャンバス上に残しておきたい、という意図と思われる(現状は整列した瞬間にしか原点の位置がわからず、後から見返しても基準がどこだったか分からなくなるため)。

**操作方法**：他のノード(エビデンス・疑問・結論・メモ)と同様に、左のツールバーに専用の追加ボタンを置き、クリックしてから図上の置きたい位置をクリックすると設置される(既存の`add-paper`等の`data-tool`と同じ操作方法)。

**確定した仕様**：
- ラベル文字列は固定(「Year」「Citation」)。自由入力・変更は不可
- 矢印の向きは固定(右向き＝Year、下向き＝Citation)。他の向きには対応しない
- プロパティパネルは持たせない(色変更・文字サイズ変更等は不可)。ただし選択してDelete/Backspaceキーで削除は可能(既存の`deleteNode()`のキーボードショートカットが全ノード種別に共通で効くため、追加実装なしでそのまま使える)
- ドラッグでの移動は可能(既存ノードと同じドラッグ移動の仕組みを流用)
- **拡大縮小(リサイズ)可能**：他のノードと同じ右下ドラッグハンドルをそのまま流用し、幅/高さに応じて矢印の長さ・ラベル位置も追従して伸縮する(2026-07-26追記：当初「リサイズ不要」としていたが、他ノードと共通のリサイズハンドルがそのまま使えるため対応することにした)
- 地図に1つまで(疑問ノード・結論ノードと同様の設置数制限)

**次回セッションでやること**：上記の確定仕様で実装する。

## 29. 画像貼り付け機能（2026-07-26 セッション、仕様確定・未実装）

Yujinさんからの提案。重要なメタアナリシスの結果(フォレストプロットなど)の画像を地図上に貼り付けたい、という用途。

**背景・懸念点**：パスファインダーはサーバーを持たず、地図データはブラウザのLocal Storageに保存する仕組みのため、画像をBase64で埋め込むとすぐに数百KB〜数MBになり、Local Storageの容量上限(ブラウザにもよるが数MB程度)に近づきやすい。この懸念を踏まえて仕様を相談し、以下の内容で確定した。

**確定した仕様**：
1. 画像ノードは1つの地図につき**5枚まで**設置可能
2. 貼り付けた画像は**自動的に縮小・圧縮してから保存**する(容量超過のリスクを抑えるため。大きな画像を貼っても一定サイズ以下に自動調整される)
3. 残数表示は、**画像追加のツールバーボタンに常時表示**する(例：「画像追加(2/5)」のように、ボタン自体に残り枚数を表示し続ける)

**確定した仕様(続き、2026-07-26追記)**：
4. 画像の取り込み操作は**Ctrl+Vでの貼り付け**：画像をコピーした状態で地図上(キャンバス)にフォーカスがある状態でCtrl+Vすると、画面中央付近に画像ノードとして自動追加される(他のノードのような「ボタン→クリックで設置」の流れは取らない)

**実装時に判断する技術的詳細(ユーザー確認不要、実装側で妥当な値を採用)**：
- 自動縮小・圧縮の基準：最大の縦横pxサイズと画質は実装時に妥当な値を採用する(容量を抑えつつ見た目を保てる値)
- 画像ノードの見た目：アスペクト比を保持して表示。他ノードと同じ右下ドラッグハンドルでリサイズ可能(縦横比は維持)
- PNG書き出し・既存のメモ展開との整合性：通常のノードと同様にノードレイヤーの一部として描画するため、PNG書き出し時も自動的に反映される

## 30. 個別ノードのDOI/PMID取得ボタンに重複チェックがない不具合（2026-07-26 セッション、指摘・未実装）

Yujinさんからの指摘で発覚。現状、DOI/PMIDによる書誌情報の取得は機能によって重複チェックの有無が異なる。

- **複数追加**機能・**Citrailからの受け取り**(連携インポート)：`findExistingPaperNode()`による重複チェックあり(DOI・PMID・OpenAlex IDのいずれか一致でスキップ)
- **個別ノードのプロパティパネルの「取得」ボタン**(`fetchAndApplyIdentifier()`)：重複チェックが**ない**。他のノードに同じDOI/PMIDが既にあっても、そのまま今操作中のノードに上書き取得されてしまい、同じ論文のノードが2つできる可能性がある

**対応方針(未実装)**：`fetchAndApplyIdentifier()`でもOpenAlexから取得した後に`findExistingPaperNode()`相当のチェックを行い、自分自身以外の既存ノードと一致した場合は警告する(上書きするかどうかの確認、または取得を中止するなど、具体的な挙動は実装前に確認すること)。

## 31. 疑問・結論・メモノードで文字がノード幅からはみ出るバグ（2026-07-26 セッション、原因特定済み・未実装）

Yujinさんからの指摘：疑問ノード・結論ノード・メモノードで、文字が折り返されずノードの外にはみ出る。「省略せず折り返すようにしてほしい」とのこと(エビデンスノードは対象外、この3種類のみ)。

**原因を特定済み**：`wrapLabelLines()`は文字数ベースで折り返し行数・折り返し文字数を計算しているが、1文字あたりの幅を`charWidth = 7.2 * fontScale`(12pxフォント時7.2px)と見積もっている。これは日本語の全角文字の実際の幅(12pxフォント時で概ね12px前後、フォントサイズにほぼ等しい)よりかなり狭い見積もりのため、実際にはノード幅に収まらない文字数まで1行に詰め込んでしまい、結果として日本語の文章では行がノードの外へはみ出る。Playwrightで実際に長い日本語文を疑問ノードに入力して検証し、テキストのバウンディングボックスがノードの図形(shape)の幅を超えることを確認済み(例：ノード幅170pxに対しテキスト幅256px)。なお`nodeInsideExcerpt()`で10文字に切り詰めて表示するエビデンスノードは、そもそも短いため今回のはみ出し問題の対象外。

**やりたいこと(Yujinさんの要望)**：
1. 折り返し計算の文字幅見積もりを、日本語の全角文字の実際の幅に近づけて修正し、ノード幅からはみ出さないようにする
2. 現在の「収まる行数を超えたら`…`で省略する」仕様をやめ、超えた分は省略せずに折り返す(＝行数の上限を設けない。結果としてノードの高さより下にはみ出ることは許容する)

**次回セッションでの実装方針(未実装)**：
- `wrapLabelLines()`のcharWidth見積もりを修正(全角文字想定でfontScale比例の値に、または実測(`getComputedTextLength`等)ベースに変更)
- 疑問・結論・メモノードについては、`wrapLabelLines()`の`maxLines`による省略(`…`付与)を行わず、必要な行数をすべて表示するよう分岐を追加(エビデンスノードの表示(タイトル外側表示・ノード内PMID/抜粋)は現状のまま変更しない)

## 32. 全ノードのメモを一括表示/非表示するボタン（2026-07-26 セッション、仕様確定・未実装）

Yujinさんからの提案：「全てのノードのメモを一括で表示・非表示させるボタン」。

**現状**：エビデンスノードのメモ展開は、ノードごとに個別にダブルクリックして開閉する仕組み(`toggleMemoExpand()`、開閉状態は`expandedMemoIds`にノードIDごとに保持、保存はされない一時的な状態)。地図全体をまとめて開閉するボタンは現状ない。

**確定した仕様**：
- ツールバーに1つのトグルボタンを置き、「全て表示」「全て非表示」を切り替える(押すとメモがあるノード全てが展開され、もう一度押すと全て閉じる)
- 一括表示中も、個別のノードをダブルクリックすれば単独で開閉できる(既存の個別操作は残す)

## 33. 疑問・結論・メモノードもワンクリックで詳細パネルの表示/非表示を切り替えられるようにする（2026-07-26 セッション、Yujinさんからの提案・未実装）

Yujinさんからの提案：「疑問、結論、メモノードも、エビデンスノードと同様にワンクリックで詳細画面が表示、非表示されるようにする」。

**現状**：エビデンスノードは`handlePaperNodeClick()`/`togglePanelForNode()`により、ドラッグを伴わないワンクリックで詳細パネルの表示/非表示が切り替わる(ダブルクリックはメモの展開/折りたたみ)。一方、疑問・結論・メモノードは`mouseup`時に`else render();`となっており(該当コード：`window.addEventListener('mouseup', ...)`内)、クリックしてもパネルの開閉状態は一切変化しない(選択状態が変わるのみ)。

**対応方針**：疑問・結論・メモノードには、エビデンスノードで使っているダブルクリック判定(メモ展開)は不要なので、単純にドラッグを伴わないクリック時に`togglePanelForNode()`を呼ぶよう変更すればよい(ダブルクリック用の遅延判定は不要)。

## 34. PNG書き出しに、表示中のグリッドとメモ展開が反映されない（2026-07-26 セッション、原因特定済み・未実装）

Yujinさんからの指摘：現時点でのPNG保存は、画面に表示されているメモ(展開表示)やグリッドが反映されない。

**原因を特定済み(`exportPng()`)**：
- グリッド：書き出し用のSVGは常に真っ白な背景(`bg`要素、`fill:'#ffffff'`)を新規に描画しており、画面のグリッドパターン(`#gridBg`)は元々クローンしていない。そのため`gridVisible`がtrueで画面にグリッドが表示されていても、書き出し結果には反映されない
- メモ展開：ノード一式を複製する際に`nodeClone.querySelectorAll('.memo-expand-box').forEach(function(el){ el.remove(); });`で展開中のメモ枠を明示的に全て削除しているため、画面上でメモを展開表示していても書き出し結果には反映されない

**対応方針(未実装)**：
- グリッド：`gridVisible`がtrueの場合、白背景の代わりに(または白背景の上に)グリッドパターンをクローンして書き出し用SVGに含める
- メモ展開：`.memo-expand-box`を無条件に削除するのをやめ、クローンして含める。ただし展開ウィンドウの✕(閉じる)ボタンやリサイズハンドルなど、操作用のUI部品は書き出し結果に写り込むと不自然なため、それらの要素だけは削除してから含める

## 35. 実装完了（2026-07-26 セッション、続き。セクション25・28〜34を一括実装）

上記の未実装だったTODO(セクション25の4項目、28、29、30、31、32、33、34)を全てまとめて実装した。あわせて、手動リンクの色プリセットの並び順をノードの色プリセットに揃える改修(ノードと矢印で色の順番を「グレー・青・緑・黄・赤・紫」に統一。先頭色が緑→グレーに変わったため、新規の手動リンクの初期色もグレーになる)も実施した。

**実装内容**：
1. **セクション25(4項目)**：ノード上のPMID表示から「PMID」の文字を除去(`nodeInsideExcerpt()`)。ツールバー文言変更(「エビデンス追加」→「ノード」、「疑問を追加」→「疑問」、「結論を追加」→「結論」、「メモ追加」→「メモ」、「自動リンク」→「引用関係」、「年代整列」→「自動整列」)。キャンバスの何もない場所をクリックすると詳細パネルが閉じるようにした(`select`ツールでの背景クリック時に`panelWrapEl`へ`collapsed`を追加)。固定チェックボックスのアイコンを📌から🔒に変更(`lockRowHtml()`)
2. **セクション33**：疑問・結論・メモノードもワンクリックで詳細パネルの表示/非表示を切り替えられるようにした。`mouseup`ハンドラで、エビデンス以外のノードには`togglePanelForNode()`を直接呼ぶよう変更(ダブルクリック判定は不要なため`handlePaperNodeClick()`は使わない)。あわせて`wasAlreadySelectedAndPanelOpen`の判定条件からエビデンス限定の制約を外し、全ノード種別で正しく機能するようにした
3. **セクション30**：`fetchAndApplyIdentifier()`(個別ノードのDOI/PMID取得ボタン)にも`findExistingPaperNode()`による重複チェックを追加(第2引数で自分自身は除外)。既存の別ノードと一致した場合は確認ダイアログを出し、キャンセルすれば上書きしない
4. **セクション31**：`wrapLabelLines()`の折り返し計算を、文字数ベースから「全角文字はほぼ正方形・半角文字はその半分」という実際の見た目の幅ベースの計算に変更(`charDisplayWidth()`)。これによりノード幅からのはみ出しを解消。あわせて疑問・結論・メモノードは、ノードの高さに収まる行数を超えても省略(`…`)せず全文を折り返して表示するように変更(`opts.noTruncate`。エビデンスノードの表示は従来通り)
5. **セクション34**：`exportPng()`で、`gridVisible`がtrueならグリッドパターンを書き出し用SVGにも重ねて含めるようにした。また`.memo-expand-box`を無条件に削除するのをやめ、閉じるボタン(`.close-btn`)とリサイズハンドル(`.memo-expand-resize-handle`)だけを取り除いてクローンし、展開中のメモが書き出し結果にも反映されるようにした(export用のスタイルに`.memo-expand-content`関連のCSSを追加)
6. **セクション32**：ツールバーに「メモ一括」ボタン(`#btn-toggle-all-memos`)を追加。押すとメモが入っている全エビデンスノードのメモ展開を一括表示し、もう一度押すと全て非表示にする(`toggleAllMemos()`)。一括表示中も個別ノードのダブルクリックでの単独開閉は引き続き可能
7. **セクション28**：新しいノード種別`axis`を追加。ツールバー「原点」ボタン→キャンバスクリックで設置(地図に1つまで)。L字型の矢印(右向き＝Year、下向き＝Citation、ラベルは固定)を表示する専用の描画関数(`renderAxisNodeContents()`)を用意し、通常のノード形状描画とは分離した。プロパティパネルは持たせず(選択時は説明文のみ表示)、ドラッグでの移動・右下ハンドルでのリサイズ(矢印の長さがノード幅/高さに追従)には対応。削除は選択してDelete/Backspaceキーで行う(既存の`deleteNode()`のショートカットがそのまま効く)
8. **セクション29**：新しいノード種別`image`を追加。操作方法はCtrl+Vでの貼り付け(`document.addEventListener('paste', ...)`)。クリップボードの画像をFileReaderで読み込み、Canvas経由で最大800pxに縮小・JPEG圧縮(quality 0.82)してからBase64のdata URLとしてノードに保存する。1つの地図につき5枚まで(超過時はアラートで通知)。ツールバーに残数を常時表示する要素(`#image-count-display`、「画像 ◯/5」)を追加。アスペクト比を保持して表示し、右下ハンドルでのリサイズも縦横比を保ったまま行われる(横方向のドラッグ量を基準に計算)。プロパティパネルは説明文と削除ボタンのみ

Playwrightで、各項目を個別に動作確認(PMID表示・ツールバー文言・パネル自動クローズ・ロックアイコン・疑問/結論/メモのワンクリック切替・重複チェックの確認ダイアログ・文字はみ出し解消・PNG書き出しへのグリッド/メモ反映・メモ一括表示非表示・原点軸アノテーションの設置/移動/リサイズ/削除/PNG反映・画像の貼り付け/上限/リサイズ/削除)した上で、既存の回帰テスト一式(round2〜round9c、ui-improvements、resize-layout-png、maps、dedup、delete×3、review-boundary、doi-test、doi-pmid-batch、Citrail連携の被引用数受け渡し)もすべて実行し、問題がないことを確認済み。

**PR #21マージ後に発覚した不具合と修正**：
- 原点・軸アノテーションのクリック判定用の透明な当たり判定(hitbox)が、右の「Year」・下の「Citation」ラベルの表示範囲(pointer-events:noneのため素通りする)をカバーしきれておらず、ラベル付近をクリックすると背景クリック扱いになり詳細パネルが開かない不具合があった。hitboxに上下左右の余白を持たせて修正済み
- 「PNGで保存」ボタンを押しても無反応になる不具合があった(Yujinさんからの報告)。原因は、メモ展開中(foreignObject/HTML使用)の状態でPNG書き出しを行うと、`canvas.toBlob()`が"Tainted canvases may not be exported"エラーで失敗し、書き出し処理全体が(エラー表示もされないまま)止まっていたため。書き出し用のメモ展開枠は、foreignObjectを使わず純粋なSVGの`text`要素として描き直す(`buildMemoExportGroup()`)ことで回避した。画像ノード(`data:`URIの`<image>`)はcanvasを汚染しないため対象外で問題なし
